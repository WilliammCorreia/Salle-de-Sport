export interface UserFilters {
  role?: string;
  isActive?: boolean;
  search?: string;
}

export interface GymHallFilters {
  status?: string;
  city?: string;
  activityType?: string;
  owner?: string;
  search?: string;
}
